# 复制成 app/.env 后填自己的 key（双击根目录的 setup.bat 会自动帮你生成这份文件）。

# ---- 语音转文字（必填）----
# 智谱 GLM-ASR。申请地址：https://open.bigmodel.cn/usercenter/apikeys
# 新账号有免费额度，glm-asr 按音频秒数计费，日常用几乎花不动钱。
ZHIPU_API_KEY=

# ---- 语音播报（不需要 key）----
# 播报走微软 Edge-TTS，免费、免登录、需要联网，不用在这里填任何东西。

# ---- 自己复跑评测（可选）----
# 只有要跑 app/testsets/ 里的评测集时才需要：跑器用 SDK 直连模型，不经过 DSH Web。
# 日常用「启动.bat」那条链路不需要这一行。
# DEEPSEEK_API_KEY=
