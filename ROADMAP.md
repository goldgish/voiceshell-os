# 语音 Coding 项目 · 方向对齐与路线图 v2

> 2026-09-11。目标形态：**任务式纯语音闭环**——遥控器一句话下任务 → agent 自主跑完 → 语音汇报结果；中间尽量不打扰，只在必须确认时语音询问。
> agent 底座仍用 DSH，**重构的是我们这侧的交互层**（DSH 原生形态是键盘对话框，不适配语音）。

## 一、现状盘点与问题诊断

| 层 | 现状 | 诊断 |
|---|---|---|
| 1. 硬件与语音监听 | ✅ 已跑通：link.py（ATVV/BLE，自动重连+心跳）→ GLM-ASR 转写约 1s | 可用基线，仅剩加固项 |
| 2. 与 agent 交互 | ⚠️ 链路已通（dsh_client.py 直连 DSH Host，prompt/follow 流式），**但形态错**：沿用 DSH 键盘对话框范式 | **核心痛点：确认交互太重**——agent 频繁反问/要确认，语音场景每次打断成本极高；回复是长篇聊天文本，不适配语音 |
| 3. agent 汇报 + 语音应答 | ❌ 未开始 | 与层 2 重构一体设计 |

**结论：层 1 保留，层 2 重构（任务式交互层），层 3 新建（TTS 播报），2+3 合并为同一个"语音交互层"设计。**

## 二、目标交互形态（任务式）

```
idle
  │ 按住语音键下任务（"帮我把 X 重构一下"）
  ▼
task_running ──agent 自主执行──┐
  │                            │（不打断：常规工具调用/进度）
  │ 必须确认项（如删文件、    │
  │ 不可逆操作、信息真缺失）   │
  ▼                            │
must_confirm：TTS 播问题 +     │
  浮窗常显 → 用户语音应答      │
  → 答案发回会话 ──────────────┘
  │ agent 完成
  ▼
done：TTS 播报结果汇报（一两句话）→ 20s 后隐窗
```

设计原则：**默认自主、最少打断**。确认是例外而非常态。

## 三、层 2 重构设计（交互层，dsh_client 不动）

重构发生在 app.py 的编排层，新增"交互编排器"概念（可先做在 app.py 内，不急着抽文件）：

### 3.1 自主化优先（解决"确认交互太重"）

1. **DSH 权限配置探测**：bundle 已确认存在 permission 设置体系（`permission` 命名空间）。P0 运行时探测可用配置项，把常规工具调用（读写文件、跑命令、跑测试）设为自动批准，从源头消除大部分确认。
2. **prompt 包装层**：发给 DSH 的每段语音转写统一加语音场景指令前缀，大意："你在语音交互场景，用户看不到屏幕细节。请自主执行，不要反问澄清，合理假设直接做；仅当操作不可逆或信息硬缺失时才提问。完成后用一两句话汇报结果。"包装对用户不可见，浮窗仍只显示用户原话。
3. **必须确认才走语音问答**：无法消除的确认（不可逆操作等）→ TTS 播报问题 + 浮窗常显（取消自动隐藏）→ 用户按语音键应答 → 转写发回同一会话。

### 3.2 面向语音的输出约定

- 约定 agent 收尾输出**简短结果汇报**（由 3.1 的指令约束）；TTS 播报取回复末尾汇报段（规则解析：最后一段或首两句），不播全文。
- 浮窗从"聊天气泡"演进为**任务卡片**（当前任务 + 状态 + 结果摘要，历史折叠）——P1，P0 先沿用现有气泡。

### 3.3 会话模型

默认单会话连续复用（DSH 天然带上下文，已验证）；语音指令"新任务/新会话"开新会话（P2）。

## 四、层 3 设计（TTS 播报）

### 4.1 播报触发点（对齐任务式）

| 触发点 | 内容 | 必播/节流 |
|---|---|---|
| 任务完成 | 结果汇报段（3.2） | 必播 |
| 必须确认项 | 问题原文 | 必播，浮窗常显 |
| 错误/失败 | 一句话原因 | 必播 |
| 关键进度节点 | 如"正在跑测试" | 节流（10s 最多一条，P1） |

信息源：follow 流帧。assistant-stream（start/chunk/end）已知；**event 帧 schema 未知**（权限请求/提问的形态），P0 第一步加全量 event 日志跑真实任务收集样本后定解析规则。

### 4.2 TTS 引擎并列对比（暂不定，P0 实测后选）

| 引擎 | 音质 | 延迟 | 成本 | 依赖 |
|---|---|---|---|---|
| Edge-TTS | 好（中文音色自然） | ~300ms 起流 | 免费 | 联网 + edge-tts 库 |
| 智谱 GLM-TTS | 好 | 待实测 | 按量计费 | 已有 ZHIPU_API_KEY |
| Windows SAPI | 机械 | ~0 | 免费离线 | 系统自带 |
| 火山/阿里 | 好 | 低 | 按量计费 | 需新申请 key |

设计：抽象 `tts.py`（`speak(text)` / `stop()`），引擎可切换；P0 用最快跑通者（倾向 Edge-TTS）验证闭环再横向对比。

### 4.3 播放与打断

- sounddevice/pygame 内存播放，不经系统播放器。
- **打断即应答**：播报中按下语音键 → 立即停止播报进入录音态（复用 stream_start 路径），这是闭环体验关键。
- 播报与浮窗渲染互不阻塞。

## 五、分阶段路线图

- **P0 · 自主化 + TTS MVP**
  1. follow 流全量 event 日志，收集确认/权限/节点样本 → 定解析规则
  2. DSH 权限自动批准配置探测与落地
  3. prompt 包装层（语音场景指令前缀）
  4. tts.py 抽象 + Edge-TTS 跑通：任务完成 → 播报结果汇报段
  5. 播报中按键打断
- **P1 · 确认闭环 + 任务态 UI**
  6. 必须确认项识别 → TTS 问 → 浮窗常显 → 语音应答发回
  7. 关键节点播报（节流）
  8. 浮窗任务卡片化
- **P2 · 体验与加固**
  9. TTS 引擎横向实测定型；LLM 摘要压缩（可选）；音色/语速配置
  10. 语音指令（新会话/停止当前任务/重播）
  11. 断连边界实测、历史持久化、计划任务化自启（miremote 经验避 UAC）

## 六、参考项目取舍（miremote-vibe）

已取：ATVV 协议层、ctypes/DPI/打包坑清单。可取：本地 whisper 备选、计划任务自启。不取：VAD、哑键注入、映射 GUI（与纯语音对话方向无关）。TTS 无参考，新建。

## 七、风险与开放问题

1. **prompt 包装约束力有限**：agent 是否真能做到"少反问、自主做"依赖模型配合，需实测调优指令措辞；权限自动批准是兜底主力。
2. **DSH event 帧 schema 未知**：运行时探测兜底，不阻塞 TTS MVP 并行。
3. **DSH alpha API 易变**：dsh_client 单文件适配层，变更收敛一处。
4. **开放问题**：长任务是否定期主动播报进度？多会话语音切换是否 P2 后真需要？

## 八、执行动作（本计划批准后）

将本文档发布为项目根目录 `ROADMAP.md`（与 PROGRESS.md 并存：PROGRESS 记"做到哪"，ROADMAP 记"往哪走"），并把 P0 五项追加到 PROGRESS.md 待办区。
